name: Frontend CI

on:
  push:
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: frontend

jobs:
  ci:
    name: lint / typecheck / build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      # TODO [48]: enable once Vitest is wired up
      # - name: Test
      #   run: npm test -- --run

# Update Log — 2026-05-07 11:11:52

## 变更来源

> 以下内容来自 scaffold-project-update.md（已原文保留）

```markdown
## 变更说明 — 2026-05-07（修订 Copilot 初版脚手架的质量问题）

本次更新是对 Copilot 初版脚手架的全面质量修订，目的是消除技术栈冲突、文档不一致和 SQL schema 隐患，让后续 do-next 工作流可以正常运转。

### 一、技术栈修正
- 前端 WebSocket：删除 Socket.io-client，改用浏览器原生 WebSocket API
- 后端 WebSocket：删除 Tokio-tungstenite，改用 actix-web-actors
- 前端数据获取：删除 SWR，仅保留 TanStack Query
- 前端认证：删除 Next Auth（与自建 JWT 后端冲突）

### 二、README.md 与 PRD/ARCHITECTURE 不一致修正
- README 核心功能补充：邀请码注册、管理员系统
- 术语统一："实时文字通话" → "实时文字消息" / "实时聊天"
- 登录字段统一：删除 users.username，仅保留 email + nickname
- 测试覆盖率统一：后端 >80%、前端 >60%

### 三、SQL Schema 修正
- users：删除 username 字段；account_code 改 CHAR(10) + CHECK 数字约束
- friend_requests：删除表级 UNIQUE，改为 pending 部分唯一索引
- message_emojis：emoji_id 添加外键 REFERENCES emojis(id) ON DELETE CASCADE
- messages：新增 idx_messages_conversation 双向复合索引

### 四、context.md 文件索引修正
- docs/API.md、docs/DATABASE.md、docs/DEPLOYMENT.md 加"（阶段 7 生成）"备注

### 五、TODO.md 格式重构
- 全文加 -- [N] 编号（51 条任务），可被 do-next、plan-todo-insert 命令识别
- 阶段 0 第一个任务的 cargo init 命令修正

### 六、ADR 表述修正
- ADR-001 标题加"后端"二字消歧义

### 七、跨文件细节修正
- TODO.md 阶段 0 task 1 的 cargo init 命令修正
- 项目名 ichatapp 在文档中保持一致（不重命名为目录名 ichatpp）
```

---

## 影响范围分析

| 文件 | 变更原因 |
|------|----------|
| README.md | 补充邀请码/管理员功能描述；术语"通话"→"消息"/"聊天" |
| PRD.md | 术语统一；用户字段说明（email + nickname，删除 username）；测试覆盖率分前后端；版本号 v0.1→v0.2；KPI 描述补全 |
| ARCHITECTURE.md | 技术栈：删 Socket.io-client/SWR/Next Auth/Tokio-tungstenite，加 TanStack Query/原生 WebSocket/actix-web-actors；SQL schema 4 处修正（users、friend_requests、message_emojis、messages 索引）；ADR-001 标题修正 |
| context.md | 项目快速摘要更新；技术约束补充新栈；docs/* 文件加"待生成"备注；末尾更新日期标注 |
| TODO.md | 全文加 -- [N] 编号（51 条）；多处任务说明同步新技术栈与新 schema（如 actix-web-actors、原生 WebSocket、emojis 表先建、双向复合索引 EXPLAIN 验证、pending 唯一索引边界等） |

---

## 实际修改摘要

### README.md
- 副标题"文字通话和消息记录" → "文字消息和聊天记录"
- 项目简介段补充"用户必须凭管理员发放的邀请码注册"
- 核心功能列表增加：邀请码注册、管理员系统、邮箱密码登录、账号码自动分配
- 章节"实时通话" → "实时聊天"

### PRD.md
- 版本号 v0.1 → v0.2，最后更新 2026-05-07
- 3.1 用户管理：明确"使用邮箱+密码登录（不使用 username 字段）"，账号码举例改为 10 位数字
- 3.1 子标题"实时通话" → "实时聊天"
- 3.2 扩展功能"通话历史统计分析" → "聊天历史统计分析"
- 3.3 非功能需求测试覆盖率拆分：后端 >80%、前端 >60%
- KPI 阶段 0-3 补充"邀请码"功能项
- 文件末尾追加 v0.2 版本历史条目

### ARCHITECTURE.md
- 第 2 节前端表：删 SWR/Socket.io-client/Next Auth；TanStack Query 单列；新增"浏览器原生 WebSocket API"行
- 第 2 节后端表：Tokio-tungstenite → actix-web-actors
- 第 5 节 users 表：删 username 字段；account_code 改 `CHAR(10) NOT NULL UNIQUE CHECK (account_code ~ '^[0-9]{10}$')`
- 第 5 节 friend_requests 表：删 `UNIQUE(from_user_id, to_user_id)`；新增部分唯一索引 `uq_friend_requests_pending`
- 第 5 节表创建顺序：emojis 提到 message_emojis 之前
- 第 5 节 message_emojis 表：emoji_id 添加 `REFERENCES emojis(id) ON DELETE CASCADE`
- 第 5 节索引：新增 `idx_messages_conversation` 与 `idx_messages_conversation_reverse` 双向复合索引
- 第 9 节 ADR-001 标题加"后端"二字

### context.md
- 项目快速摘要补充：登录方式（email+password）、账号码（10 位数字）、TanStack Query、原生 WebSocket、actix-web-actors、不使用 Socket.io / Next Auth
- 技术约束新增：前端数据获取（TanStack Query）、前端 WebSocket（原生）、前端认证（不用 Next Auth）、后端 WebSocket（actix-web-actors）
- 参考文件索引：docs/API.md、docs/DATABASE.md、docs/DEPLOYMENT.md 加"待生成，见 TODO 阶段 7"备注
- 文件末尾标注 2026-05-07 同步 scaffold-project-update

### TODO.md
- 全文重构：所有任务加 `-- [N] [ ]` 编号，编号 1-51 连续
- 任务 [1]：`cargo init --name ichatapp-backend` → `cargo init backend --name ichatapp-backend`，依赖列表新增 actix-web-actors
- 任务 [2]：建表顺序提示（emojis 在 message_emojis 之前）；users 无 username 字段；friend_requests 用部分唯一索引；新增 idx_messages_conversation 双向复合索引验收
- 任务 [3]：账号码生成需符合 CHECK 约束 `^[0-9]{10}$`
- 任务 [4]：`create-next-app` 加 `--app` 参数；安装 TanStack Query；明确"不安装 socket.io-client、SWR、next-auth"
- 任务 [5]：lib/websocket.ts 使用原生 WebSocket API
- 任务 [6]：注册请求体不含 username
- 任务 [10]：邀请码验证：明文输入，后端 SHA-256 哈希后查询
- 任务 [11]：注册表单字段不含 username
- 任务 [14]：返回不含 password_hash
- 任务 [19]：好友请求依赖 uq_friend_requests_pending 索引；上次被拒绝后能再次发送（验收）
- 任务 [27]：使用 actix-web-actors 而非 tokio-tungstenite
- 任务 [30]：利用 idx_messages_conversation 复合索引；EXPLAIN 验收
- 任务 [33]：使用浏览器原生 WebSocket；TanStack Query 缓存联动
- 任务 [38]：依赖外键 ON DELETE CASCADE 自动清理 message_emojis
- 任务 [42]：API hooks 测试明确 TanStack Query
- 任务 [43]：复合索引命中验证（EXPLAIN ANALYZE）
- 任务 [47]：完成后可移除 context.md 中的"待生成"标签
- 文件末尾"下一个待处理任务"指向 -- [1]

---

## 未修改文件

- **PENDING.md**：保持初始状态 `## still thinking, do not touch`，本次未涉及内容变更
- **scaffold-project-update.md**：本身就是变更入口，不算被修改的目标，但流程结束后会重置为初始注释

---

## 待后续单独处理（本次范围之外的发现）

以下问题在本次评估中被发现，但未包含在本次更新范围内，建议后续单独写入 scaffold-project-update.md 处理：

1. **邀请码存储方式自相矛盾**：ARCHITECTURE.md 4.1/4.6 节说"SHA-256 哈希存储"，但 SQL schema 中 `invitations.code` 注释为 `INV-{20位随机字符}` 似乎是明文。需要明确：哈希存储则不能展示明文（生成时一次性返回），明文存储则要面对数据库泄露风险。
2. **目录名 ichatpp vs 文档项目名 ichatapp**：本次保留 ichatapp，但目录仍是 ichatpp。如果用户希望统一，下次更新可以选择重命名目录或重命名文档。
3. **token 存储方式**：目前 TODO 任务 [12] 写的是 localStorage，从 XSS 安全角度讲 httpOnly cookie 更优。是否切换需要决定。

---

*生成时间：2026-05-07 11:11:52*
*操作者：Claude（scaffold-project-update）*

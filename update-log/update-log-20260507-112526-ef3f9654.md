# Update Log — 2026-05-07 11:25:26

## 变更来源

> 以下内容来自 scaffold-project-update.md（已原文保留）

```markdown
## 变更说明 — 2026-05-07（处理上一轮 update-log 末尾遗留的三项问题）

本次更新处理三个独立但都触及多个文件的关键决策。

### 一、邀请码采用 SHA-256 哈希存储（消除文档 vs SQL schema 的矛盾）
- SQL invitations 表：code → code_hash CHAR(64) + code_prefix VARCHAR(8)
- 索引：idx_invitations_code → idx_invitations_code_hash
- API：明文仅在生成响应一次性返回；列表接口返回 prefix + masked
- DELETE :code → DELETE :id（按 UUID）
- validate 端点改 POST，明文走请求体不走 URL

### 二、项目名 ichatapp → ichatpp（与目录名 c:\projects\ichatpp 统一）
- 全局 replace_all 5 个文件
- TODO.md 中 Cargo 包名 ichatapp-backend → ichatpp-backend

### 三、Token 存储改为 httpOnly cookies（XSS 安全加固）
- access_token / refresh_token：httpOnly + Secure + SameSite=Lax
- 新增 csrf_token cookie（非 httpOnly）+ 双提交 cookie CSRF 防护
- 新增 POST /api/auth/refresh、POST /api/auth/logout 端点
- 前端 fetch 必须 credentials:'include'，mutation 附带 X-CSRF-Token
- WebSocket 升级握手改为读 cookie 验证身份
```

---

## 影响范围分析

| 文件 | 邀请码哈希 | 项目改名 | httpOnly cookies |
|------|-----------|----------|------------------|
| README.md | — | ✓ | — |
| PRD.md | ✓ 第 6 节、版本 v0.3 | ✓ | — |
| ARCHITECTURE.md | ✓ 4.1/4.6/schema/索引 | ✓ | ✓ 4.1/4.7/6/8 |
| context.md | ✓ 必须遵守列表 | ✓ | ✓ 技术约束、必须遵守、禁止事项 |
| TODO.md | ✓ tasks 2/8/9/10/13 | ✓ Cargo 包名 | ✓ tasks 5/7/11/12/27 + 新增 [52] |

---

## 实际修改摘要

### README.md
- 全局 replace_all：`ichatapp` → `ichatpp`（标题与正文若干处）

### PRD.md（v0.2 → v0.3）
- 全局 replace_all：`ichatapp` → `ichatpp`
- 第 6 节"邀请码设计决策"：明确 SHA-256 哈希存储；CSV 下载仅在生成时点；管理员页面只能看 masked 视图
- 文档信息表版本号 v0.2 → v0.3
- 末尾追加 v0.3 版本历史条目

### ARCHITECTURE.md
- 全局 replace_all：`ichatapp` → `ichatpp`
- 4.1 认证模块：
  - 技术方案补充三件套 cookie 配置（access_token / refresh_token / csrf_token）+ CSRF 双提交说明
  - 注册流程末步从"生成 JWT 返回客户端"改为"通过 Set-Cookie 写入；响应体不返回 token"
  - API 端点表新增 /api/auth/refresh、/api/auth/logout、/api/invitations/validate（POST）；改 DELETE :code → :id
- 4.6 邀请码模块：
  - 验证流程改为"明文 → SHA-256 → 查 code_hash"
  - 新增"存储约定"小节
  - API 端点全面重写：generate 一次性返回明文；list 返回 prefix + masked，不返回 code_hash；DELETE 按 id；validate 改 POST 走请求体
- 4.7 WebSocket：连接生命周期第 1 步改为"浏览器自动携带 access_token cookie，握手时验证"
- 5 SQL schema：
  - invitations 表 `code VARCHAR(30)` → `code_hash CHAR(64)` + 新增 `code_prefix VARCHAR(8)`
  - 索引 idx_invitations_code → idx_invitations_code_hash
- 6 API 设计原则：
  - "认证：JWT (Bearer Token)" → "JWT 通过 httpOnly cookie 自动携带（不使用 Bearer 头）"
  - 新增 CORS 必须 `Allow-Credentials: true` 且 `Allow-Origin` 不可用通配符的说明
- 8 安全考虑：
  - 认证与授权：补充 cookie 三件套配置说明
  - 输入验证：补充 CSRF 双提交 cookie 防护说明、XSS 与 cookie 互动说明
- 文末"最后更新"标注本次同步

### context.md
- 全局 replace_all：`ichatapp` → `ichatpp`
- 技术约束："前端认证"行重写为 cookie + CSRF 描述
- AI 工作规范"必须遵守"列表新增两条：
  - 邀请码 SHA-256 哈希存储 + 明文一次性返回
  - token 只能存 httpOnly cookie；禁止经 Authorization 头/localStorage/sessionStorage/非 httpOnly cookie 暴露
  - 所有 mutation 必须经 CSRF 中间件
- "禁止事项"列表新增：禁止把 token 写入任何前端可读存储；禁止响应体下发 token
- 文末"最后更新"标注本次同步

### TODO.md
- 全局 replace_all：`ichatapp` → `ichatpp`（含 Cargo 包名 ichatapp-backend → ichatpp-backend）
- 任务 [2]：迁移脚本提示 invitations 用 code_hash + code_prefix；索引名 idx_invitations_code_hash
- 任务 [5]：lib/api.ts 重写说明——强制 credentials:'include'；不读取/存储 token；自动附加 X-CSRF-Token；401 走 /api/auth/refresh
- 任务 [7]：从单一 login 端点扩展为 login + refresh + logout 三个端点；全部基于 Set-Cookie；register 同步改造
- 任务 [8]：明文格式说明 + SHA-256 → code_hash + 前 8 位 → code_prefix；明文一次性返回
- 任务 [9]：列表只返回 prefix + masked；不返回 code_hash
- 任务 [10]：改 POST，明文走请求体；后端 SHA-256 后查 code_hash
- 任务 [11]：注册页改用 POST /api/invitations/validate；不读取/保存 token
- 任务 [12]：登录页 fetch credentials:'include'；不写 localStorage；以 cookie 自动管理为准
- 任务 [13]：管理员邀请码列表显示 masked；DELETE 按 id；生成弹窗一次性显示明文 + 下载 CSV 提示
- 任务 [27]：WebSocket 认证从 URL/Header → 改为读 access_token cookie
- **新增任务 [52]**：实现 CSRF 中间件（双提交 cookie 方案，例外路由 login/register/invitations/validate）
- 文末标注本次同步与任务 [52] 编号说明

---

## 未修改文件

- **PENDING.md**：保持初始状态 `## still thinking, do not touch`
- **scaffold-project-update.md**：本次结束后会重置为初始注释状态

---

## 待后续单独处理（本次范围之外的发现）

1. **CSRF token 旋转策略**：当前 csrf_token cookie 与 access_token 同步过期（1h），未规定刷新时是否同步旋转 csrf_token。后续可写更新明确。
2. **WebSocket 重连时 cookie 失效处理**：access_token cookie 过期后 WS 重连会失败，前端是否应在 401 时自动调用 /api/auth/refresh 然后再重连？这块逻辑未在 TODO 中显式列出，建议补到任务 [33]。
3. **invitations.code_prefix 长度**：当前 VARCHAR(8) 适配 'INV-AbCd'。如果后续修改明文格式（如去掉 INV- 前缀），prefix 长度也要相应调整。

---

*生成时间：2026-05-07 11:25:26*
*操作者：Claude（scaffold-project-update）*

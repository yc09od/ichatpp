# Update Log — 2026-05-07 23:38:09

## 变更来源

> 以下内容来自 scaffold-project-update.md（已原文保留）

将来deploy想要配置到coolify帮我想一个可行方案

---

## 影响范围分析

| 文件 | 是否更新 | 说明 |
|---|---|---|
| ARCHITECTURE.md | ✅ | 第 7 节"部署架构"重写：新增 7.1（Coolify 拓扑）、7.2（资源清单）、7.3（环境变量与 JWT 密钥）、7.4（首次部署流程）、7.5（多实例）、7.6（备份与监控）、7.7（Nginx 备选）、7.8（容器化）|
| context.md | ✅ | 项目快速摘要补一句"生产部署目标 Coolify"；技术约束新增"生产部署平台"条目；已知问题加一行 JWT 私钥持久化注意；底部更新时间戳同步 |
| README.md | ✅ | 技术栈段把"Docker、Docker Compose"扩展为"Docker 多阶段镜像、Docker Compose（开发）、Coolify 自托管 PaaS（生产首选）" |
| PRD.md | ❌ | 部署属于技术决策，不影响产品需求 |
| TODO.md | ❌ | [52] DEPLOYMENT.md 任务已规划，实施时按 ARCHITECTURE.md §7 新方案执行；[53] 多阶段 Dockerfile 任务保持不变 |

---

## 实际修改摘要

### ARCHITECTURE.md
- §7 整体改写：从单一"Nginx + 多实例"路径，改为以 **Coolify 单域名同源**为首选、Nginx 为备选的双方案文档
- 新增 ASCII 拓扑图：Coolify Proxy（Caddy/Traefik）按 `/api/*` vs `/*` 分流到 backend / frontend，3 个数据 service（Postgres/Redis/MinIO）独立容器
- 新增"关键设计：单域名同源"小节，说明此选择如何消除 CORS 与 cookie SameSite 限制
- 新增 Coolify 配置清单表：2 个 Application（backend / frontend，从 Dockerfile 构建）+ 3 个数据资源（一键模板）+ 路由规则
- 新增环境变量速查表（backend 与 frontend 分列），明确 single-origin 下的取值
- 新增 JWT RS256 私钥的两种注入策略：持久卷挂载（推荐）vs Coolify Secrets 文件
- 新增首次部署 7 步流程：建项目 → 配置环境变量 → 终端生成 JWT 密钥 → 首次启动跑迁移 → seed_admin → MinIO 桶初始化
- 新增水平扩展、备份、监控小节，并明确 backend 已为多实例设计（Redis Pub/Sub 跨实例广播，无需 sticky session）
- 保留 Nginx + docker-compose 备选方案的旧拓扑图作为 §7.7

### context.md
- 项目快速摘要末尾补一句指向 ARCHITECTURE.md §7 的部署目标说明
- 技术约束区新增一条：生产部署平台 Coolify，单域名同源
- 已知问题表格新增"Coolify 部署中 JWT 私钥需挂载持久卷"一行（中优先级）
- 底部时间戳更新

### README.md
- 技术栈段一行扩展，明确 Docker / Docker Compose 仅开发用，Coolify 为生产首选

---

## 未修改文件

- **PRD.md**：部署是工程决策，不影响产品功能定义
- **TODO.md**：阶段 7 中 [52]（DEPLOYMENT.md）和 [53]（多阶段 Dockerfile）任务已存在；实施时按 ARCHITECTURE.md §7 新方案执行即可，无需新增条目

---

*生成时间：2026-05-07 23:38:09*
*操作者：Claude（scaffold-project-update）*

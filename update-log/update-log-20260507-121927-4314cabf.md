# Update Log — 2026-05-07 12:19:27

## 变更来源

> 以下内容来自 scaffold-project-update.md（已原文保留）

```
## 变更说明 — 2026-05-07

### 技术变更：WebSocket 实现迁移

将后端 WebSocket 实现从 **`actix-web-actors 4.x`** 迁移到 **`actix-ws 0.3.x`**。

**原因：**
- `actix-web-actors v4.3.1+deprecated`：actix 官方已在版本号尾部嵌入 `+deprecated` 标记，明确该 crate 已废弃、不再维护
- `actix-ws` 是 actix 团队当前推荐的替代：不再依赖 actor 模型，handler 直接返回 `(HttpResponse, Session, MessageStream)`，与现代 async/await 风格更契合，代码更易读

**影响范围：**
- ARCHITECTURE.md 第 2 节后端依赖表（替换 actix-web-actors → actix-ws）
- ARCHITECTURE.md 第 4.7 节 WebSocket 模块设计（改写"基于 actor 模型"相关措辞为"基于 Session + MessageStream 的 tokio task 模型"）
- context.md "技术约束 — 后端 WebSocket"（替换约束）
- TODO.md 阶段 4（[26]–[30]）所有提及"actor"的措辞，改为 actix-ws 的 Session/MessageStream 模型
- backend/Cargo.toml（删 `actix-web-actors`，加 `actix-ws`）

不影响 PRD.md（产品行为不变）、README.md（高层描述未提 actor）、数据模型（不变）。
```

---

## 影响范围分析

技术栈变更（WebSocket 实现），按 skill 的 U2 表格属于「技术栈变更」。需要同步更新：

| 文件 | 处理方式 | 备注 |
|------|----------|------|
| ARCHITECTURE.md | **修改** | 第 2 节依赖表 + 第 4.7 节实现方式说明 + 底部时间戳 |
| context.md | **修改** | 项目快速摘要、技术约束「后端 WebSocket」、底部时间戳 |
| TODO.md | **修改** | [3] 依赖列表、[26] WebSocket 握手、[27] 在线广播、[30] 心跳清理 |
| backend/Cargo.toml | **修改** | 删 actix-web-actors，加 actix-ws + dashmap |
| backend/Cargo.lock | **自动重生成** | cargo build 期间 actix-web-actors 已从依赖图剥离 |
| README.md | 未修改 | 高层技术栈描述未提 actor 实现细节 |
| PRD.md | 未修改 | 产品行为不变 |
| PENDING.md | 未修改 | 仍为占位行 |

---

## 实际修改摘要

### ARCHITECTURE.md（v 时间戳更新）

- **第 69 行（第 2 节后端依赖表）：**
  - 旧：`| actix-web-actors | 4.x | WebSocket 支持（基于 actor 模型，Actix 官方组件） |`
  - 新：`| actix-ws | 0.3.x | WebSocket 支持（轻量 Session + MessageStream，actix 团队当前推荐；不依赖 actor 模型） |`
- **第 4.7 节顶部新增「实现方式」段：**
  说明使用 `actix_ws::handle(&req, body)` 升级，启动 tokio task 处理入站流；连接状态用 struct 管理 + Redis 跨实例广播；明示「不使用 actor 模型」。
- 「连接生命周期」「事件类型」「性能优化」三段未动，原文已是 protocol-oriented 表述，与 actor 实现解耦。
- 底部时间戳追加注：「WebSocket 实现从 actix-web-actors 迁移到 actix-ws」。

### context.md

- **项目快速摘要：** `actix-web-actors（WebSocket）` → `actix-ws（WebSocket，Session + MessageStream，不使用 actor 模型）`
- **技术约束 → 后端 WebSocket：** 改为 `actix-ws 0.3.x（Session + MessageStream + tokio task 模型；不使用 actor 模型，不使用 tokio-tungstenite，不使用已废弃的 actix-web-actors）`
- 底部时间戳追加注。

### TODO.md

- **[3] 后端 Cargo 工程初始化** —— 依赖列表 `actix-web-actors 4.x` → `actix-ws 0.3.x`
- **[26] WebSocket 握手与鉴权** —— 实现方式重写为 `actix_ws::handle(&req, body)` 拿 `(HttpResponse, Session, MessageStream)`，启动 tokio task 处理入站流
- **[27] 在线状态广播** —— 引入「进程内 Session 注册表（DashMap<UserId, Vec<Sender>>）」描述，跨实例仍用 Redis Pub/Sub
- **[30] 心跳与错误处理** —— 验收标准从「清理 actor」改为「结束 Session task 并从注册表 + Redis online 集合清理」
- 底部「下一个待处理任务」前的"最后更新"追加注。

### backend/Cargo.toml

```diff
- actix-web-actors = "4.3"
+ actix-ws = "0.3"
+ dashmap = "6"  # in-process Session registry for WebSocket fan-out (TODO [27])
```

### backend/Cargo.lock

由 cargo build 自动重生成。新版本：
- 移除：`actix-web-actors 4.3.1+deprecated`
- 新增：`actix-ws 0.3.1`、`dashmap 6.1.0`、`hashbrown 0.14.5`

注：cargo 提示 `actix-ws 0.4.0` 可用但 `"0.3"` 约束限制了选择 0.3.1。0.3 → 0.4 是 breaking minor，本次保守不升。

---

## 验收

- ✅ `cargo build` exit 0（8.79s 增量）
- ✅ `cargo clippy --all-targets` exit 0（5.80s）
- ✅ Cargo.lock 中无 actix-web-actors 残留

---

## 未修改文件

- README.md（顶层描述未提 actor 模型）
- PRD.md（产品需求不变）
- PENDING.md（仅占位行）

---

*生成时间：2026-05-07 12:19:27*
*操作者：Claude（scaffold-project-update）*
